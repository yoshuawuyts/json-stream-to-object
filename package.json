{
  "name": "json-stream-to-object",
  "description": "Parse a JSON stream into an object",
  "repository": "yoshuawuyts/json-stream-to-object",
  "version": "1.0.0",
  "scripts": {
    "deps": "dependency-check . && dependency-check . --extra --no-dev",
    "start": "node .",
    "test": "standard && npm run deps && node test"
  },
  "dependencies": {
    "end-of-stream": "^1.4.0",
    "fast-json-parse": "^1.0.2",
    "is-stream": "^1.1.0",
    "request-payload": "^1.0.1"
  },
  "devDependencies": {
    "dependency-check": "^2.8.0",
    "get-server-port": "^1.0.0",
    "spok": "^0.8.1",
    "standard": "^10.0.2",
    "tape": "^4.6.3"
  },
  "keywords": [
    "stream",
    "parse",
    "json"
  ],
  "license": "MIT"
}
